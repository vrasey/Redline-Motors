/* --- Global Styles & Variables --- */
:root {
    --primary-red: #D2042D; /* A strong, sporty red */
    --dark-bg: #111111;
    --card-bg: #1E1E1E;
    --text-light: #F5F5F5;
    --text-secondary: #AAAAAA;
    --font-primary: 'Exo 2', sans-serif; /* For headings */
    --font-secondary: 'Roboto', sans-serif; /* For body text */
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: var(--font-secondary);
    background-color: var(--dark-bg);
    color: var(--text-light);
    line-height: 1.6;
}

.container {
    width: 90%;
    max-width: 1200px;
    margin: 0 auto;
}

h1, h2, h3 {
    font-family: var(--font-primary);
    font-weight: 700;
    line-height: 1.2;
}

a {
    text-decoration: none;
    color: var(--primary-red);
}

img {
    max-width: 100%;
    display: block;
}

/* --- Header & Navigation --- */
header {
    background-color: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    padding: 1rem 0;
    position: fixed;
    width: 100%;
    z-index: 1000;
    border-bottom: 1px solid var(--card-bg);
}

header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-family: var(--font-primary);
    font-size: 1.8rem;
    font-weight: 700;
    color: var(--text-light);
}

.logo span {
    color: var(--primary-red);
}

nav ul {
    list-style: none;
    display: flex;
    gap: 1.5rem;
}

nav a {
    color: var(--text-light);
    font-weight: 700;
    padding: 0.5rem 0;
    transition: color 0.3s ease;
    border-bottom: 2px solid transparent;
}

nav a:hover, nav a.active {
    color: var(--primary-red);
    border-bottom-color: var(--primary-red);
}


/* --- Hero Section --- */
.hero {
    height: 100vh;
    background-image: url('images/hero_main.jpeg');
    background-size: cover;
    background-position: center;
    position: relative;
    display: flex;
    align-items: center;
    text-align: center;
}

.hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to top, rgba(0,0,0,0.9), rgba(0,0,0,0.4));
}

.hero-content {
    position: relative;
    z-index: 2;
}

.hero h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
}

.hero p {
    font-size: 1.2rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

/* --- Buttons --- */
.btn {
    display: inline-block;
    padding: 0.8rem 2rem;
    font-family: var(--font-primary);
    font-size: 1rem;
    font-weight: 700;
    border-radius: 5px;
    transition: all 0.3s ease;
    text-transform: uppercase;
}

.btn-primary {
    background-color: var(--primary-red);
    color: white;
    border: 2px solid var(--primary-red);
}

.btn-primary:hover {
    background-color: transparent;
    color: var(--primary-red);
}

/* === REMOVED .btn-secondary as it's no longer used === */


/* --- Previous Orders Section --- */
.previous-orders {
    padding: 5rem 0;
}

.previous-orders h2 {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 3rem;
}

.order-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: 1.5rem;
}

.order-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    border-radius: 8px;
    border: 1px solid #2a2a2a;
    transition: transform 0.3s ease, opacity 0.3s ease;
}

.order-item img:hover {
    transform: scale(1.03);
    opacity: 0.85;
}

.order-item h3
{
    /* This adds the space between the image and the title */
    margin-bottom: 1rem;

    /* These are extra styles to make the text look nice */
    text-align: center;
    padding: 0 1rem 1rem; /* Adds a little breathing room on the sides and bottom */
}


/* --- Footer --- */
footer {
    background-color: #000;
    text-align: center;
    padding: 2rem 0;
    margin-top: 3rem;
    border-top: 1px solid var(--card-bg);
}

footer p {
    color: var(--text-secondary);
}

/* --- Responsive Design --- */
@media (max-width: 768px) {
	
	 header 
	 {
        /* This makes the header scroll away on mobile */
        position: static;
    }
    
    nav ul 
    {
        justify-content: center; /* This will center the links */
        gap: 1.2rem;             /* Reduces the space between each link */
        flex-wrap: wrap;         /* Good practice to allow wrapping if needed */
    }

    nav a 
    {
        font-size: 0.9rem;      /* Makes the font slightly smaller to fit */
    }
    /*ADDED THIS RULE FOR MOBILE SCALING */
    .hero 
    {
        background-attachment: fixed;
        font-size: 2.5rem;
    }

    .hero h1 
    {
        font-size: 2.5rem;
    }
    
    header .container 
    {
        flex-direction: column;
        gap: 1rem;
    }
    
    .page-header
    {
    	padding-top: 40px;
    }
}